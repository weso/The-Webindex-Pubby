#if($uri.contains("/country/"))
#parse("header-country.vm")
#else
#parse("header.vm")
	#if ($image)
	<div class="image"><img src="$image" alt="Depiction of $title" /></div>
	#end
#end

#if ($comment)
    <p>$comment</p>
#end

#if (!$properties.isEmpty())
	#if($uri.contains("/country/"))
	#parse("proptable-country.vm")
	#else
	#parse("proptable.vm")
	#end
#else
    <p>No further information is available.</p>
#end

#if ($metadata)
	<a name="meta"></a>
	<h2>Metadata</h2>
	<div id="metadata-tables">
	#parse("metadatatable.vm")
	</div>
	<a href="#meta" onclick="showAllMetadata('metadata-tables')">expand all</a>
#end

#if($uri.contains("/country/"))
#parse("footer-country.vm")
#else
#parse("footer.vm")
#end